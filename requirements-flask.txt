# Core Flask dependencies for deployment
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0

# Essential MARL dependencies
numpy==1.24.3
torch==2.0.1
gymnasium==0.29.1
pyyaml==6.0.1

# For JSON handling and utilities
requests==2.31.0
python-dateutil==2.8.2

# Optional: Lightweight versions for deployment
# Remove heavy dependencies that aren't needed for API
# gym-multigrid (will be imported dynamically)
# wandb (optional for deployment)
# matplotlib (optional for deployment)
